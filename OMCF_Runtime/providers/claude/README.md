# Claude Provider Adapter

Claude is a model provider target for future Runtime execution.

Runtime V2.6 records adapter contracts but does not enable the adapter by default.

Claude can be used later for long-context reasoning tasks after provider credentials and audit gates are configured.
