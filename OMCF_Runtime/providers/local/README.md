# Local LLM Provider Adapter

Local LLM is a provider target for private, offline, or self-hosted inference.

Runtime V2.6 keeps this adapter disabled until a local model runtime, model path, resource policy, and audit policy are configured.
