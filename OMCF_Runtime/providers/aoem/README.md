# AOEM Provider Adapter

AOEM is a runtime provider target for execution, GPU dispatch, privacy compute, and AOEM-specific tasks.

Runtime V2.6 keeps AOEM disabled by default because AOEM core execution logic requires double review and explicit human approval.

AOEM tasks must reference:

```text
MCP/16_Knowledge_Base/AOEM/AOEM_CONSTITUTION.md
```
